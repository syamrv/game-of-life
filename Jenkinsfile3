currentBuild.displayName = "Final_Demo # "+currentBuild.number
    String getCommit() {
    return sh(script: 'git rev-parse HEAD', returnStdout: true)
}

node('master'){
        env.Docker_tag = gitTagName()



		
		stage('ansible playbook'){
			sh 'echo ${Docker_tag}'
			 	script{
				    sh '''final_tag=$(echo $Docker_tag | tr -d ' ')
				     echo ${final_tag}test
				     echo $final_tag'''
				}
		
		}
	}
